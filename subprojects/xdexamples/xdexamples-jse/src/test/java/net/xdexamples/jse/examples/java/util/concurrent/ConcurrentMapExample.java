package net.xdexamples.jse.examples.java.util.concurrent;

import net.xdexamples.support.internal.BaseExample;
import xd.helpers.dummies.DummyKey;
import xd.helpers.dummies.DummyValue;

import java.util.concurrent.ConcurrentMap;

public class ConcurrentMapExample extends BaseExample<ConcurrentMap<DummyKey, DummyValue>> {
    @Override
    protected void scaffold(ConcurrentMap<DummyKey, DummyValue> instance) throws Throwable {
/*
compute
computeIfAbsent
computeIfPresent
forEach
getOrDefault
merge
putIfAbsent
remove
replace
replace
replaceAll
 */
    }
}
